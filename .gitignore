# FreeCAD backups
*.FCBak
*.FCStd1

# Large lever STL package (254 MB) -- exceeds GitHub 100 MB limit.
# Use Git LFS if you want to version this, or keep it local only.
*.stl
*.STL

# WSL/Windows Zone.Identifier alternate streams
*:Zone.Identifier

# In-progress browser downloads
*.crdownload
Unconfirmed*

# Scraped third-party site assets (may contain their own API keys -- see
# GitHub secret-scanning alert on the initial commit). The parent HTML alone
# has the info we care about; the _files/ sidecar is just CSS/JS/images.
*_files/

# Python
__pycache__/
*.pyc

# Editor
*~
.#*
\#*\#
